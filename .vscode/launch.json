{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Importer debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/cmd/importer",
        }
    ]
}
